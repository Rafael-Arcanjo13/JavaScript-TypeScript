const nomes = ['Rafael', 'Gabriela', 'Gabriel', 'Leticia', 'Gato'];

console.log(nomes)

// UNSHIFT
//nomes.splice(0, 0, 'Flavia')

// PUSH 
//nomes.splice(nomes.length, 0, 'Flavia')

// SHIFT
//const removidos = nomes.splice(0, 1)

// POP
//const removidos = nomes.splice(-1, 1)

//const removidos = nomes.splice(3,  1, 'Pudim', 'Batata')

//.splice(indice, delete, elem1, elem2, elem3)