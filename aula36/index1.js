// for in ler os indices do agrupamento

//let numeros = [20, 30, 40, 50, 60]
//
//for (let i in numeros) {
//    console.log(numeros[i])
//}

let pessoa = {
    nome: 'Rafa',
    sobrenome: 'Arcanjo',
    idade: 18
}

for (let chave in pessoa) {
    console.log(i + ': ' + pessoa[i])
}